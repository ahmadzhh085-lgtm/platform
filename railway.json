{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "dockerfile",
        "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan config:clear && php artisan route:clear && php artisan view:clear && npm install && npm run build && php artisan route:cache && php artisan view:cache"
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
        "preDeployCommand": "php artisan migrate --force && php artisan db:seed --class=RolePermissionSeeder --force && php artisan db:seed --class=AdminUserSeeder --force",
        "restartPolicyMaxRetries": 5,
        "restartPolicyWindowSeconds": 60
    },
    "volumes": [
        {
            "name": "database",
            "mountPath": "/app/database"
        },
        {
            "name": "storage",
            "mountPath": "/app/storage"
        }
    ]
}
